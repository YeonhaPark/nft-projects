import { Flex } from "@chakra-ui/react";
import { FC } from "react";
import Slider from "../components/Slider";

const Home: FC = () => {
  return (
    <Flex>
      <Slider />
    </Flex>
  );
};

export default Home;
